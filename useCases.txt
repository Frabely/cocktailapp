Bugs:
-home button should reset all filters currently in useEffect in home but not working -> assign it to header (make sure only called when actually in home -> header is used elsewhere)
-currentItem and setCurrentItem should be a redux state -> refactor -> make sure to define a empty item and set it as default type {idDrink: null}
-after logout set Home to default (no filters/buttons pressed) -> still ingredients selected (else fixed)
-ingredients state is still in Home and not in redux
-input: on click current value is selected
-shadow on android 'not' working -> see Card component
-splash new background needs to be added and replaced
-change label for password placeholder or text to show it should be at least 6 chars long
TODOs:
-refactor reducer -> change hardcode strings to const's
-languages: refactored (only dummy data and ingredients missing -> a lot of data)
    -add prop to components and get message language like <const_call>[`&{props.language}`]
-Implement profile -> change username and password:
    -expend page with additional input fields and buttons (like login sign up account)
-user profile:
    -pages:
        -profile:
            -user name unique -> set username as id in firebase?
                ->throw error when register user with already used username
        -bar/favorites ----> later create own recipes
        -settings:
            -font size?
            -color themes?
-user login:
    -modal for errors/UI messages:
        -successfully logged in/logged out
        -created account
-Firebase Backend -> upload data auf firebase
-settings page
-favorites page:
    -same like full list
        -make star on every card to make it as favorite
-testing:
    -cover as much as possible with error codes and matching messages
-change padding and margin const's to rem
-what font and default size used -> make font sizes const's
-refactor app states in store
-refactor and optimize highlight card, extract maybe
-refactor filter file, extract maybe (possible make a reusable component for header elements)
-werbung -> wie?
-add app to stores
    -create logo and splash -> already done? https://www.figma.com/file/4TD0sMutRjxBI6zKySFV2n/Expo-App-Icon-%26-Splash-(Community)?node-id=1%3A1357

Nice to have features:
-make colors changeable

Cocktail App

	•	Indirekte verstecke Werbung unterbringen, aller 15 - 20 Cocktails anzeigen —> 1 Cocktail Feld wird durch Werbefeld ersetzt

	•	Menü Leiste:
	⁃	Favoriten Liste
	⁃	Notiz/Einkaufszettel
	⁃	Eigene Bar:
	•	Eigene Bilder
	•	Eigene Zutaten


	•	Filter FilterButton Einbauen ??
	        design Filter: Und- oder OderFilter (Banane + Cola -> nur mit Banane und Cola) oder (Banane + Cola -> mit Banane oder Cola) anzeigen
	⁃	Anfangsbuchstabe
	⁃	Alkoholfreies
	⁃	Alkoholisches
	⁃	Glas Varianten:
	•	Cup
	•	Glass
	•	Cocktail glass
	•	Large glass
	•	Old Fashion glass
	•	Highball glass
	•	Shot glass
	⁃	Typ:
	•	Shot
	•	Ordinary Drink
	•	Cocktail
	•	Beer
	•	Shake
	•	Homemade liqueur
	•	Punch / Party Drink
	•	Coffee / Tea
	•	Cocoa
	⁃	Clean all 🗑

	•	Sterne Bewertung für Benutzer

	•	App Name Idee:
	⁃	Cocktaillicious
