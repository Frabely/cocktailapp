Bugs:
-if no favorites -> Favorites page shows "No Hits -- Reset Filters" -> should display something like no favorites found
-reload after star in favorites is removed -> removed card from dataSet
-on login (with other account) filter might be open -> set all filter closed on logout
-briefly showing all cocktails on favorites page load
-if All selected and gets reselected page reloads -> should not reload -> takes time that is not needed
-loading symbol behind filter
-labels to big for change password -> overflowing screen
-favorites filters load briefly all before showing filtered data
-labels on android overflowing (german filter buttons)

Master branch Goals - deploy alpha:
-(remove) flatlist from filter -> add map to iterate and scrollview to scroll if overflow -> open filter section on click like change password etc
-button/label sizes for tablet
-highlight image (to close) should be height:width 1:1 or 1.5:1
-disable language -> German not fully supported:
    -check for translation API -> maybe fast, good, free solution to translate
-set Font consts
-2€ for all cocktails
-set email language (https://firebase.google.com/docs/auth/web/manage-users?hl=de&authuser=0)
-setup GooglePlay deploy
-add app to stores:
    -adds
    -create logo and splash -> already done? https://www.figma.com/file/4TD0sMutRjxBI6zKySFV2n/Expo-App-Icon-%26-Splash-(Community)?node-id=1%3A1357
-handle screen rotation see docs
-testing start:
    -cover as much as possible with error codes and matching messages

TODOs:
-loading too much on search?
-data state to big -> laggy?
-load only 10 items and update on scroll?
-app icon rework -> add triangle
-fetch only favorites function -> don't need to fetch all when only favorites are required
-refactor login screen and settings -> put all error checking into functions -> currently way to many code
-user profile:
    -pages:
        -profile details:
            -change email
            -delete account
-rating:
    -filter
-change padding and margin const's to rem
favorites:
    -share cocktails
    -create cocktails
-refactor reducer -> change hardcode strings to const's
-user profile:
    -pages:
        -settings:
            -font size?
            -color themes?
-refactor and optimize highlight card, extract maybe
-refactor filter file, extract maybe
-what font and default size used -> make font sizes const's
-abo

Nice to have features:
-highlight card full screen:
    -swipe down to get next cocktail
-use mobile turn 90deg function
-make colors changeable
-change label for password placeholder or text to show it should be at least 6 chars long
-shadow on android 'not' working -> see Card component
-(python-)script to delete all not verified accounts in db as well as in firebase auth console
-profile picture

Cocktail App

	•	Indirekte verstecke Werbung unterbringen, aller 15 - 20 Cocktails anzeigen —> 1 Cocktail Feld wird durch Werbefeld ersetzt

	•	Menü Leiste:
	⁃	Favoriten Liste
	⁃	Notiz/Einkaufszettel
	⁃	Eigene Bar:
	•	Eigene Bilder
	•	Eigene Zutaten


	•	Filter FilterButton Einbauen ??
	        design Filter: Und- oder OderFilter (Banane + Cola -> nur mit Banane und Cola) oder (Banane + Cola -> mit Banane oder Cola) anzeigen
	⁃	Anfangsbuchstabe
	⁃	Alkoholfreies
	⁃	Alkoholisches
	⁃	Glas Varianten:
	•	Cup
	•	Glass
	•	Cocktail glass
	•	Large glass
	•	Old Fashion glass
	•	Highball glass
	•	Shot glass
	⁃	Typ:
	•	Shot
	•	Ordinary Drink
	•	Cocktail
	•	Beer
	•	Shake
	•	Homemade liqueur
	•	Punch / Party Drink
	•	Coffee / Tea
	•	Cocoa
	⁃	Clean all 🗑

	•	Sterne Bewertung für Benutzer

	•	App Name Idee:
	⁃	Cocktaillicious
